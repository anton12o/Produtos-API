services:
  db:
    image: postgres:15
    environment:
      - POSTGRES_USER=usuario1
      - POSTGRES_PASSWORD=senha
      - POSTGRES_DB=produtos_db
    ports:
      - "5432:5432"

  api:              
    build: .         
    ports:
      - "8000:8000"
    depends_on:
      - db